title: Plant Growing
views:
  - title: Kiffzimmer Grow
    path: plant_growing
    icon: mdi:cannabis
    type: masonry
    cards:
      # === HEADER ===
      - type: markdown
        content: |
          # 🌿 Kiffzimmer Grow Station

      # === PRESENCE ===
      - type: entities
        title: 📍 Room Status
        show_header_toggle: false
        state_color: true
        entities:
          - entity: binary_sensor.kiffzimmer_true_presence
            name: Room Presence
            icon: mdi:motion-sensor

      # === EQUIPMENT ===
      - type: entities
        title: ⚙️ Equipment
        show_header_toggle: false
        entities:
          - entity: light.kiffzimmer_lichtstreifen
            name: Grow Light
            icon: mdi:led-strip-variant
          - entity: light.computer_licht
            name: Room Light
            icon: mdi:lightbulb
          - entity: cover.yoga_blind_vorhang
            name: Window Blind
            icon: mdi:blinds-horizontal

      # === QUICK ACTIONS ===
      - type: horizontal-stack
        cards:
          - type: button
            name: Work Mode
            icon: mdi:lightbulb-on
            tap_action:
              action: call-service
              service: light.turn_on
              data:
                entity_id: light.kiffzimmer_lichtstreifen
                brightness_pct: 100

          - type: button
            name: Stealth
            icon: mdi:eye-off
            tap_action:
              action: call-service
              service: script.safe_close_blind
              data:
                cover_entity: cover.yoga_blind_vorhang
                tracker_entity: input_boolean.blind_yoga_last_opened

      # === INFO ===
      - type: markdown
        content: |
          ### 📋 Optimal Ranges
          🌱 Seed: 20-25°C, 65-70%
          🌿 Veg: 22-28°C, 40-70%
          🌸 Flower: 20-26°C, 40-50%

      # === AUTOMATION STATUS ===
      - type: entities
        title: 🤖 Automation
        show_header_toggle: false
        entities:
          - entity: input_boolean.auto_lights_enabled
            name: Auto Lights
          - entity: input_boolean.auto_shutters_enabled
            name: Auto Blinds
